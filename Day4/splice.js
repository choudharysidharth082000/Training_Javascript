const  a = ["Sidharth", "Choudhary", "Hello world"];

a.splice(0,2,"Anushka");
//first one is the start index second one is the upto which point you want to splice and third arguement is what should be placed inside that
console.log(a);
console.log(a[0]);

//task to get the index whose index is 1